"""Framework integrations that ship with firm-audit.

Nothing here is imported by ``firm.audit`` itself, so importing firm-audit never drags in a web
framework: import the submodule you want (today: ``firm.audit.contrib.django``).
"""
