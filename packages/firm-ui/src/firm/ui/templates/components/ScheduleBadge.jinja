{#def expr, desc="" #}
{# `desc` is the human-readable cron description precomputed in render.py ("" when unknown). #}
<span class="pill mono"{% if desc %} title="{{ desc }}"{% endif %}>{{ expr }}</span>
