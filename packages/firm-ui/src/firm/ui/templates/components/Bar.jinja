{#def pct #}
{# `pct` arrives already clamped to 0–100 by render.py; the `--fill` variable feeds the
   `width` rule in style.css (`.bar i`) — no styling lives here, only the datum. #}
<span class="bar"><i style="--fill:{{ pct }}%"></i></span>
